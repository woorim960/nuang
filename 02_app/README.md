# NUANG App Workspace

이 폴더는 실제 제품 코드가 들어갈 영역이다.

예정 앱:

```text
02_app/nuang-web
```

원칙:

- `00_source_packages`는 원본 보존 영역으로 유지한다.
- `01_project_control`은 기획·결정·게이트 문서 영역으로 유지한다.
- 앱 코드는 문서와 분리해 `02_app` 아래에서 관리한다.
- 최초 앱은 Next.js App Router 기반 모바일 웹/PWA로 만든다.
