# Local release evidence

Place hashed legal approvals, store-console confirmations, real-device QA JSON,
and screenshots referenced by `config/mobile-release-evidence.json` in this
directory. The files are intentionally ignored; only this guide is committed.

Do not place signing keys, passwords, provisioning credentials, production
personal data, or unredacted account documents here.
