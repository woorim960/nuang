# Mobile local assets

`nuang-character-purple.webp` is copied from the canonical web asset during the mobile build setup. Store builds must package this asset locally and must not depend on `nuang.app` for the boot screen.
