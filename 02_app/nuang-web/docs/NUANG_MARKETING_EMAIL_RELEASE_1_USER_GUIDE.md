# 뉴앙 마케팅 이메일 Release 1 — 운영 시작 가이드

작성일: 2026-08-03
대상: 뉴앙 운영자

## 지금까지 완료된 것

- 발신 표시: `뉴앙 <news@notice.nuang.app>`
- 답장·고객 문의: `woorimprog@gmail.com`
- 문의 연락처: `010-2515-0939`
- 이메일 채널만 사용하고 SMS는 잠금
- 로그인 화면과 마이 설정의 `광고성 이메일 수신 동의 (선택)` 통일
- 구버전 동의, 미인증 이메일, 정지·삭제·병합 확인 중 계정 자동 제외
- 제목의 `(광고)`, 발신자·문의 정보, 로그인 없는 수신거부 자동 삽입
- 이메일 앱의 원클릭 수신거부 헤더 지원
- 반송·스팸 신고 자동 차단
- 중복 방지, 오전 8시~오후 9시 발송, 최대 5회 재시도
- 2년 주기 수신동의 상태 안내 예약
- 운영센터 `/admin/marketing`에서 초안·미리보기·테스트·승인·예약·중지·재개·취소·성과 관리
- Vercel 운영 환경에 발신 정보 등록
- 실제 대량 발송 스위치는 `false`로 안전하게 잠금

`news@notice.nuang.app` 받은편지함을 별도로 만들 필요는 없다. Resend에서 인증된 `notice.nuang.app`의 발신 전용 주소로 사용하며, 사용자가 답장하면 `woorimprog@gmail.com`으로 전달된다. Resend 무료 플랜의 1개 도메인 한도에도 맞는다.

## 현재 적용 상태

- `202608030003_marketing_email_release1.sql`: 운영 DB 적용 완료
- `202608030004_business_operations_control_plane.sql`: 운영 DB 적용 완료
- 운영자 시험 메일: 발송·전달 완료
- Resend Webhook: `email.delivered` 수신·DB 매칭 완료
- DB 긴급 중지: 중지·재개·감사 기록 확인 후 정상 상태로 복구
- 안전 재시도: 공급자 미접수 실패가 없을 때 마케팅·광고 모두 실행 거부 확인
- 실제 대량 발송 Gate: 계속 안전 잠금

## 운영자가 지금 해야 할 일

필수 작업은 없다. `woorimprog@gmail.com` 받은편지함에서 제목이 `(광고) 뉴앙 이메일 운영 점검`인 시험 메일을 눈으로 한 번 확인하면 된다. 시스템은 이미 해당 메일의 전달 완료를 확인했다.

실제 회원 대상 캠페인을 시작하기 전에는 아래 순서만 지킨다.

1. 운영센터에서 실제 캠페인 초안과 운영자 테스트를 완료한다.
2. 현재 버전의 `전달 확인` 표시를 확인한다.
3. 첫 발송 규모와 시각을 정한다.
4. 그때만 `MARKETING_EMAIL_SEND_ENABLED=true`로 전환하고 재배포한다.
5. 첫 소규모 발송의 전달·반송·수신거부 지표를 확인한 뒤 확대한다.

## 첫 실제 캠페인 운영 순서

1. 운영센터의 `이메일 캠페인`에서 초안을 작성한다.
2. `운영자 테스트`를 눌러 본인 메일에서 확인한다.
3. 제목과 본문, 링크를 다시 읽고 `승인`한다.
4. 예약 시각을 고르거나 바로 대기열에 넣는다.
5. 발송 중 이상이 보이면 즉시 `일시중지`한다.
6. 전달·반송·신고·수신거부·실패 수치를 확인한다.

운영자는 회원의 동의를 대신 켤 수 없고 원문 이메일 목록을 내려받을 수 없다. 캠페인 대상을 확정한 뒤에도 실제 발송 직전에 동의와 인증 상태를 다시 확인한다.

## 법률 확인 기준

변호사나 별도 기관의 사전 승인을 받아야만 이메일을 보낼 수 있는 구조는 아니다. Release 1은 대한민국 현행 법령과 KISA 안내에 맞춘 기술 Gate를 적용했다. 다만 아래 상황에서는 외부 법률 검토를 권장한다.

- SMS·앱 푸시 등 새 광고 채널을 열 때
- 오후 9시 이후 광고 발송을 검토할 때
- 해외 사용자를 대상으로 현지 마케팅을 시작할 때
- 민감정보나 제3자 데이터를 광고 대상 선정에 사용하려 할 때
- 법령 또는 KISA 안내가 크게 바뀔 때

이 내부 판단 과정이나 법률 검토 여부를 사용자에게 별도 경고 문구로 노출하지 않는다. 사용자에게는 명확한 선택권과 정상적으로 동작하는 설정만 제공한다.
